program calculator;
var
a,b,c : integer;
d: real;
t, f: boolean;
ch : char;
str: string;

begin
   a:=21;
   b:=10;
   c := a + b;
   t := true;
   f := false;
   ch := 'c';
   str := 'rafael';

   c := a - b;
   c := a * b;
   d := a / b;
   c := a mod b;
   c := a div b;

end.
